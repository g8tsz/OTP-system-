# API base URL. Leave empty to use the in-browser mock service (demo mode).
VITE_API_URL=

# Show demo hints and use predictable OTP in mock mode (default: true in dev).
VITE_DEMO_MODE=true

# OTP configuration
VITE_OTP_LENGTH=6
VITE_RESEND_COOLDOWN_SECONDS=30
VITE_OTP_EXPIRY_MINUTES=10
